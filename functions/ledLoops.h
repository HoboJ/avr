//Header file for ledLoops.c

void loopLeft (int delay);      //Led's loop to the left with variable delay

void loopRight (int delay);     //Led's loop to the right with variable delay

void loopLeftRight (int delay); //Led's rotate back and forth with variable delay

void loopX (int delay);         //Led's form an X with variable delay
